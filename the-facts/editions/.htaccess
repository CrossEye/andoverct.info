# Restore normal DirectoryIndex behavior for edition subdirectories.
# (The parent .htaccess sets DirectoryIndex _dispatch.php for the rewrite,
# but inside editions/ we want each subdirectory's index.html to serve.)
DirectoryIndex index.html index.php

# .htaccess in /reports/aes/preschool-funding/ (or anywhere upstream)

# Tell browsers that .md files are UTF-8 plain text
AddType "text/plain; charset=utf-8" .md

# Also helpful: declare UTF-8 as the default for any text/* response
# that doesn't already specify a charset
AddDefaultCharset utf-8
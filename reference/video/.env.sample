# Copy this file to `.env` and fill in your values. `.env` is gitignored.
#   cp .env.sample .env
#
# Deepgram API key — used by transcribe-deepgram.js.
# Get one at https://console.deepgram.com/  (Settings → API Keys).
DEEPGRAM_API_KEY=

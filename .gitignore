# Dependencies
node_modules/

# Secrets & local environment
.env
.env.local
*.local

# Private working notes / build scratch (kept out of the repo)
.meta/

# External GitHub Pages TiddlyWikis — maintained as their OWN repos, served on
# subdomains (charter22 / charter24 / flyer .andoverct.info). The hub only
# links to them; it does not track or deploy them.
Charter/

# TownMeetings heavy/secret artifacts living under reference/video/
# (also covered by reference/video/.gitignore brought in via subtree).
# The 4.2 GB of source audio/video is never committed.
reference/video/node_modules/
reference/video/output/video/
reference/video/temp/
reference/video/.env

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

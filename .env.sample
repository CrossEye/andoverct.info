# Copy to .env (gitignored) and fill in. Used by `npm run deploy -- --deploy`.
#
# FTP password for user `crosseye` on pup.phpwebhosting.com (web root /www).
# Plain FTP, port 21, no encryption — see _build/site.manifest.json and the
# deploy notes. Host/port/user live in the manifest; only the password is secret.
FTP_PASSWORD=
